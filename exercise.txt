exercise 1.
Hãy tưởng tượng có 1 folder mới được tạo. Hãy liệt kê các file theo từng vùng, liệt kê các commit và message tương ứng sau khi thao tác một loạt các lệnh sau
a. tạo 3 file: file1, file2, file3
b. chạy lệnh `git init`
c. chạy lệnh git commit -m "init project"
d. chạy lệnh git add file1
e. chạy lệnh git commit -m "add file"
f. chạy lệnh git add .
g. chạy lệnh git restore --staged file2
h. chạy lệnh git commit -m "add all files"
i. chạy lệnh git reset HEAD~1

---

a. tạo 3 file: file1, file2, file3
- vùng local: file1, file2, file3
- vùng working directory: ko
- vùng staging: ko
- vùng repository: ko
b. chạy lệnh `git init`
- vùng local:
- vùng working directory: file1, file2, file3
- vùng staging: ko
- vùng repository: ko
c. chạy lệnh git commit -m "init project"
- vùng local:
- vùng working directory: file1, file2, file3
- vùng staging: ko
- vùng repository: ko
d. chạy lệnh git add file1
- vùng local:
- vùng working directory: file2, file3
- vùng staging: file1
- vùng repository: ko
e. chạy lệnh git commit -m "add file"
- vùng local:
- vùng working directory: file2, file3
- vùng staging: ko
- vùng repository: file1
f. chạy lệnh git add .
- vùng local:
- vùng working directory: ko
- vùng staging: file2, file3
- vùng repository: file1
g. chạy lệnh git restore --staged file2
- vùng local:
- vùng working directory: file2
- vùng staging: file3
- vùng repository: file1
h. chạy lệnh git commit -m "add all files"
- vùng local:
- vùng working directory: file2
- vùng staging: ko
- vùng repository: file1, file3
i. chạy lệnh git reset HEAD~1
- vùng local:
- vùng working directory: file2, file3
- vùng staging: ko
- vùng repository: file1